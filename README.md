```
cd finscore
pip install requirements.txt
streamlit run app.py
```

#### bits-project-final-year.ipynb is to generate csv files. If you only want to check credit score simulation, no need to run it. Just run the streamlit app.